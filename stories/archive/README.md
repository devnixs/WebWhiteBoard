# Story Archive

Move older `_COMPLETE` stories here when the active `stories/` directory becomes noisy.

- Do not archive `_TODO` or `_IN_PROGRESS` stories.
- Keep filenames unchanged when moving completed stories so links and story numbers remain stable.
- Update `stories/00-story-map.md` and run `npm run validate:workflow` after moving stories.
